{
  "name": "url-shortner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsx src/main.ts",
    "dev:up": "docker-compose up --build -d",
    "dev:down": "docker-compose down --volumes",
    "db:migrate": "npx prisma migrate dev",
    "db:migrate:docker": "docker exec -it url-shortener-app npx prisma migrate dev --name init",
    "db:deploy": "npx prisma migrate deploy",
    "db:generate": "npx prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "fastify": "^5.4.0",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.13",
    "prisma": "^6.11.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}